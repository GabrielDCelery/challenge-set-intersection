## Context

For full context see `README.md` and the `docs/` directory — they contain the requirements, design decisions, entity definitions, testing strategy, and architecture. The code in `internal/` implements what is described there.
